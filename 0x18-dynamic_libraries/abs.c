#include "main.h"

/**
 * _abs - function name
 *
 * Description: computes the absolute value of an integer
 *
 * @a : parameter to check
 * Return: Always 0
 */
int _abs(int a)
{

	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
