#include "main.h"

/**
 * _abs - function name
 *
 * Description: computes the absolute value of an integer
 *
 * Return: Always 0
 */
int _abs(int)
{
	int a;

	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
